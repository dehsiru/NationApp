import { CountryWithMaxRatio } from './country-with-max-ratio';

describe('CountryWithMaxRatio', () => {
  it('should create an instance', () => {
    expect(new CountryWithMaxRatio()).toBeTruthy();
  });
});
